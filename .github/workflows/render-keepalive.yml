name: Keep Render Alive

on:
  schedule:
    - cron: '*/14 * * * *' # Se ejecuta cada 14 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          curl -I ${{ secrets.RENDER_BACKEND_URL }}/health